/target
**/*.rs.bk
.DS_Store

# napi-rs build artifacts — per-platform .node binaries are not tracked,
# but the loader shim (index.js) and type defs (index.d.ts) are.
crates/html-extractor-napi/*.node
crates/html-extractor-napi/npm/
node_modules/
